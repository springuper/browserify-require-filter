{
  "name": "browserify-css-filter",
  "version": "0.1.0",
  "description": "browserify transform for filtering require css expression",
  "main": "index.js",
  "scripts": {
    "test": "npm run lint && tap test/*.js",
    "cov": "tap --cov test/*.js",
    "lint": "eslint *.js 'lib/**/*.js' test/*.js bin/*.js",
    "coveralls": "COVERALLS_REPO_TOKEN=REPO_TOKEN npm run cov"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/springuper/browserify-require-filter.git"
  },
  "keywords": [
    "browserify",
    "require",
    "css",
    "filter"
  ],
  "author": "springuper <springuper@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/springuper/browserify-require-filter/issues"
  },
  "homepage": "https://github.com/springuper/browserify-require-filter#readme",
  "devDependencies": {
    "eslint": "^2.1.0",
    "sink-transform": "^1.0.0",
    "tap": "^5.0.0"
  },
  "dependencies": {
    "browserify-transform-tools": "^1.5.3"
  }
}
